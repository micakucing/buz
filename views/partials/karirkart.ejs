 
<% 
if(!datas) {}else{
        %>
    <%- include('m_care') %>

        <div class="wrpsx" style="    position: relative;
    height: 450px;
    overflow: hidden; ">

            <div style="position: relative;" class="backgrounds" data-type="background"></div>

            <div class="title_k">
                <%- kars%>
                    <div class="langs">
                        <ul>
                            <li><a class="en" href="?clang=en" data-id="En">English</a></li>
                            <li><a class="id" href="?clang=id" data-id="ind">Indonesia</a></li>
                        </ul>
                    </div>
            </div>
        </div>

        <% } %>


            <div class="konten_karir">

                <% 
if(!datas) {
        %>

                    <div class="nopage"><i id="fro" class="fa fa-frown-o"></i>
                        <%=terjemahan.GALAT%>
                    </div>
                    <% 
    }else{
        %>

                        <% 
    }
        %>
                            <% 
    if(datas){
        %>
                                <%    datas.forEach(function( a ) { 
%>
                                    <div class="wrp_cars">

                                        <div class="l_car">
                                            <div class="title_krs">
                                                <%= a.nama_karir %>
                                            </div>
                                            <div class="time_krs">
                                                <%= a.tangal_publis %>
                                            </div>

                                        </div>

                                        <div class="r_car" style="position: relative;">
                                            <%- a.deskripsi_karir %>
   <div class="applys" data-idp="<%= a.id_karir %>" style="position: absolute;
    bottom: 10px;
    right: 10px;
    background: #bb883c;
    padding: 5px;
    font-weight: bold;
    font-size: 13px;
    border: 1px solid #bfbfbf;
    border-radius: 5px;
    text-shadow: none; cursor: pointer;"><%=terjemahan.LAMAR%></div>
            </div>
                                        </div>

                                    </div>
                                    <%
        })
        %>


                                        <% 
    }
    %>
            </div>
 
 