 
    <%- include('m_dp') %>

<% 
    if(!datas){
        %>
    <div class="nopage"><i id="fro" class="fa fa-frown-o"></i><%=terjemahan.GALAT%></div>

    <%
    }else{
        %>
        <main id="gallery" class="main" style="padding: 10px;">


            <%
             var i = 0;
    datas.forEach(function( a ) { 
         
    if(a.imgurl){
       ig = '/potogaleryimage/' + a.imgurl;
        h = ''
    }else{
        ig = '/assetimage/noa.jpg';
      
    }
        
        %>

                <figure class="figure1 imghvr-slide-up" >
                    <img class="figure-img" src="https://www.xbuz.co.id/potogaleryimage/<%= a.imgurl %>" alt="">
    <a  class="lop" data-num=<%= i %>  data-caption="<%= a.deskrip %>" data-width="1200" data-height="900" itemprop="count" href="https://www.xbuz.co.id/potogaleryimage/<%= a.imgurl %>"  data-gallery="gallery">
<figcaption class="tp_g">
                         <div class="t_ml"> <%= a.deskrip %></div>
  </figcaption></a>
                     
                </figure>

    <a href="#_" class="lightbox" id="img<%= i %>">
  <img src="<%= ig %>">
                     <div class="descriptions">
                        <h1><%= a.deskrip %></h1>
                     </div>
</a>
                <%
                  i++
    }) 
      
    %>

        </main>
        <%
 }%>


            <div class="paggbtn_c">
                <% if (pageCount > 1) { %>
                    <ul class="pagination">
                        <% var i = 1;
	            if (currentPage > 5) {
	                i = +currentPage - 4;
	            } %>
                            <% if (i !== 1) { %>
                                <li class="disabled" style=" width: 50px;
    border-radius: 3px;"><div  data-v="<%= num %>" data-p="1" id="linp">...</div></li>
                                <% } %>
                                    <% for (i; i<=pageCount; i++) { %>
                                        <% if (currentPage == i) { %>
                                            <li class="active"><span><%= i %> <span class="sr-only">(current)</span></span>
                                            </li>
                                            <% } else { %>
                                                <li>
                                                    <div  data-v="<%= num %>" data-p="<%= i %>" id="linp" >
                                                        <%= i %>
                                                    </div>
                                                </li>
                                                <% } %>
                                                    <% if (i == (+currentPage + 4)) { %>
                                                        <li class="disabled" style=" width: 50px;
    border-radius: 3px;"><div id="linp" data-v="<%= num %>" data-p="<%= pageCount %>" >...</div></li>
                                                        <% break; } %>
                                                            <% } %>

                    </ul>
                    <% } %>
            </div>
